(FUTURE)
For improving context of related files, we need to consider:

Next steps (if you want them next)
2-hop proximity (follow deps of deps)
Import graph for C# (parse using + project structure / .csproj)
Sliding windows: include only the region around the relevant function/class instead of entire files
Recency working set: pull recently edited files from SQLite and include small snippets/summaries as candidates