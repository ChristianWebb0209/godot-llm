the chat for viewing the conversation between user and llm is completely broken. after a response, the response and user message disappear compeltely. the two dropdowns above for thinking history and change history are completely broken. think of it like this: the chat is one long continous stream of data. that data should probably be stored in a json and resolved by a custom resolver. the two dropdowns are elements that appear IN the chat, in the appropriate part of the message. the messages scroll, and the user can scroll all the way to the top of chat just like in cursor.